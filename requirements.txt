# Log Sentinel AI — Server Requirements
# Install with: pip install -r requirements.txt

# Backend (FastAPI + ML)
fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
scikit-learn>=1.4
pyarrow>=15.0
joblib>=1.3
numpy>=1.26
azure-cosmos>=4.5

# Frontend (Flask)
flask>=3.0
requests>=2.31
python-dotenv>=1.0

# Windows Event Log (Windows only — skip on Linux)
# pywin32>=306
